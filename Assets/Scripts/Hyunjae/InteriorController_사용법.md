# InteriorController 사용 가이드

## 🎯 이 스크립트는 무엇을 하나요?

**InteriorController**는 **가게 인테리어 시스템**을 담당하는 스크립트입니다. 
스프라이트 이미지를 사용해서 가게 안에 가구나 장식품 같은 인테리어를 배치하고 관리할 수 있습니다.

## 📋 간단한 설명

### 1. **인테리어 부품 등록**
   - 스프라이트 이미지를 "인테리어 부품"으로 등록합니다
   - 예: 의자 스프라이트, 테이블 스프라이트, 꽃병 스프라이트 등

### 2. **인테리어 배치**
   - 등록한 부품을 원하는 위치에 배치합니다
   - 그리드 시스템으로 깔끔하게 정렬됩니다

### 3. **인테리어 관리**
   - 배치한 인테리어를 제거하거나 관리할 수 있습니다

---

## 🚀 사용 방법

### Step 1: 스크립트 추가
1. Unity에서 빈 GameObject를 생성합니다
2. `InteriorController` 컴포넌트를 추가합니다

### Step 2: 스프라이트 설정
1. Inspector 창에서 **"인테리어 부품 설정"** 섹션을 찾습니다
2. **"Interior Parts"** 리스트의 크기를 늘립니다 (예: 3개)
3. 각 항목에 다음을 설정합니다:
   - **Part Name**: 부품 이름 (예: "의자", "테이블", "꽃병")
   - **Sprite**: 사용할 스프라이트 이미지
   - **Size**: 부품 크기 (선택사항)
   - **Sorting Order**: 렌더링 순서 (숫자가 클수록 앞에 표시)

### Step 3: 테스트하기

#### 방법 1: 마우스 클릭으로 배치 (자동 활성화됨)
- 게임 실행 후 **마우스 왼쪽 클릭**: 첫 번째 인테리어 부품 배치
- **마우스 오른쪽 클릭**: 모든 인테리어 제거

#### 방법 2: 코드로 배치
```csharp
// 다른 스크립트에서 사용 예시
InteriorController controller = GetComponent<InteriorController>();

// 인덱스로 배치
controller.PlaceInterior(0, new Vector3(5, 3, 0));

// 이름으로 배치
controller.PlaceInteriorByName("의자", new Vector3(2, 1, 0));
```

#### 방법 3: Context Menu 사용
- Inspector에서 컴포넌트 우클릭 → **"데모: 인테리어 자동 배치"** 선택
- 자동으로 격자 패턴으로 인테리어가 배치됩니다

---

## 📝 주요 메서드 설명

### `PlaceInterior(int partIndex, Vector3 position)`
- 인덱스로 인테리어 부품을 배치합니다
- **partIndex**: Interior Parts 리스트의 인덱스 (0부터 시작)
- **position**: 배치할 위치 (월드 좌표)

### `PlaceInteriorByName(string partName, Vector3 position)`
- 이름으로 인테리어 부품을 배치합니다
- **partName**: 부품 이름 (Part Name과 일치해야 함)

### `RemoveInterior(GameObject interiorObj)`
- 특정 인테리어를 제거합니다

### `ClearAllInteriors()`
- 모든 배치된 인테리어를 제거합니다

### `AddInteriorPart(string partName, Sprite sprite, Vector2 size, int sortingOrder)`
- 런타임에 새로운 인테리어 부품을 추가합니다

---

## 💡 실제 사용 예시

### 예시 1: 가게 인테리어 초기화
```csharp
void Start()
{
    InteriorController controller = GetComponent<InteriorController>();
    
    // 가게 중앙에 테이블 배치
    controller.PlaceInteriorByName("테이블", new Vector3(0, 0, 0));
    
    // 테이블 주변에 의자 배치
    controller.PlaceInteriorByName("의자", new Vector3(-2, 0, 0));
    controller.PlaceInteriorByName("의자", new Vector3(2, 0, 0));
    
    // 벽에 장식품 배치
    controller.PlaceInteriorByName("꽃병", new Vector3(0, 5, 0));
}
```

### 예시 2: UI 버튼으로 인테리어 배치
```csharp
public InteriorController interiorController;
public Button chairButton;

void Start()
{
    chairButton.onClick.AddListener(() => {
        // 버튼 클릭 시 의자 배치
        Vector3 randomPos = new Vector3(
            Random.Range(-5f, 5f),
            Random.Range(-5f, 5f),
            0
        );
        interiorController.PlaceInteriorByName("의자", randomPos);
    });
}
```

---

## ⚙️ 설정 옵션

- **Interior Parent**: 인테리어들이 배치될 부모 오브젝트 (자동 생성됨)
- **Sorting Layer Name**: 렌더링 레이어 이름 (기본값: "Default")
- **Grid Size**: 그리드 크기 (기본값: 1.0) - 배치 시 이 크기 단위로 정렬됨

---

## 🎮 테스트 팁

1. **빠른 테스트**: 게임 실행 후 마우스 왼쪽 클릭으로 바로 테스트 가능
2. **자동 배치**: Inspector에서 우클릭 → "데모: 인테리어 자동 배치"로 패턴 확인
3. **디버그**: Console 창에서 배치 로그 확인 가능

---

## ❓ 문제 해결

**Q: 인테리어가 화면에 안 보여요**
- 스프라이트가 제대로 설정되었는지 확인
- Camera의 위치와 크기 확인
- Sorting Layer가 올바른지 확인

**Q: 마우스 클릭이 작동하지 않아요**
- Camera에 Main Camera 태그가 있는지 확인
- 2D 게임인 경우 Camera 설정 확인

**Q: 인테리어가 겹쳐서 보여요**
- Sorting Order 값을 조정하세요 (숫자가 클수록 앞에 표시)



